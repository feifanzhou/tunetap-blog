<% provide(:title, @post.title_text) %>
<% provide(:og_image, @post.image_url) unless @post.image_url.blank? %>
<% provide(:og_title, @post.title_text) %>
<canvas class='BkgBlur' src='<%= @post.image_url %>'></canvas>
<%= render 'shared/search_bar' %>
<%= render_post_partial(@post, @vote, false, @is_logged_in) %>